import {
    createSlice
} from "@reduxjs/toolkit";

const noteReducer = createSlice({
    name:`noteReducer`,
    initialState:[],
    reducers:{
        
    }
})
export const {

} = noteReducer.actions
export default noteReducer.reducer